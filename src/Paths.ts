import { PathType } from "./Types";

export const pathGenerator = (path: PathType) => {
  // const inst = path.inst?.id;
  // const coll = path.coll?.id;
  // const obj = path.obj?.id;
  // if (inst && !coll && !obj) return `/institutions/${inst}`;
  // if (inst && coll && !obj) return `/collections/${coll}`;
  // if (inst && coll && obj) return `/objects/${obj}`;
  // else return "/";
};
